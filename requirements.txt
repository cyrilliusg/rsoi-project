Django==5.2.5
uvicorn==0.35.0
python-dotenv==1.1.1
psycopg[binary]==3.2.9
djangorestframework==3.16.1
gunicorn==23.0.0

# Phase 2: JWT validation (used by car/rental/payment/gateway).
# IdP also imports cryptography directly for key handling.
PyJWT[crypto]==2.10.1
cryptography==44.0.0
requests==2.32.5

# Phase 1: IdP-only (other services don't need it but harmless in CI).
django-cors-headers==4.6.0

# Phase 4: Kafka producer (gateway) + consumer (statistics-service).
# `kafka-python==2.0.2` is broken on Python 3.12 (ModuleNotFoundError:
# kafka.vendor.six.moves — its vendored six relies on the removed `imp`
# module). kafka-python-ng is the maintained fork with the same import
# path (`from kafka import KafkaProducer`).
kafka-python-ng==2.2.3

# Shared authlib package (editable install so CI sees source changes).
-e services/shared/authlib
